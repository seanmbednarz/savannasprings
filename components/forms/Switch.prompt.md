Toggle for binary settings (e.g. "Enroll in auto-renew rental", "Text me reminders").

```jsx
<Switch label="Auto-renew my rental" defaultChecked />
<Switch label="Paperless billing" size="sm" />
```

Spring-blue when on. Controlled (`checked`+`onChange`) or uncontrolled (`defaultChecked`).
